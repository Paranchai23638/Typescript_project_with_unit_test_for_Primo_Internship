# TypeScript Project: Primo Internship Assessment

This project implements a function to **merge three sorted arrays** into a single sorted array (ascending order) without using the built-in `.sort()` function. It includes a comprehensive Unit Test suite using **Jest**.

---

### 🔧 Setup Instructions

Follow these steps to set up and run the project on your local machine.

#### 1. Clone the Repository
```bash
git clone [your-repo-link]
`````
#### 2. Install Dependencies 
Install the required packages (Jest, TypeScript, ts-node):
```bash
npm install
`````
#### 3. Run Unit Tests 
Execute the test suite to verify the logic. This command compiles the TypeScript code and runs the tests defined in tests/merge.test.ts.
```bash
npm test
`````

